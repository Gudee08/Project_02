this projects shows the i/o streams, classes and objects. 
it also include some functions, loops and control statements
the input file is included (clients_info.dat) and output file movie_report.txt 
